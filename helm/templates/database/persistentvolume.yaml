apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongodb-pv
spec:
  capacity:
    storage: {{ .Values.database.storage | quote }}
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /data/mongodb