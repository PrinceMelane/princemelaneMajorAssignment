apiVersion: v1
kind: Secret
metadata:
  name: mongo-secret
type: Opaque
data:
  MONGO_USERNAME: {{ .Values.secret.MongoUsername | b64enc }}
  MONGO_PASSWORD: {{ .Values.secret.MongoPassword | b64enc }}