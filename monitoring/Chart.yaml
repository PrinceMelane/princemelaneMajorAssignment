apiVersion: v2
name: monitoring
description: Prometheus + Grafana stack for AKS
version: 0.1.0

dependencies:
  - name: kube-prometheus-stack
    version: "*"
    repository: https://prometheus-community.github.io/helm-charts